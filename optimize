cd bin

INPUT=$1
OUTPUT=$2


mv "$INPUT" t
lzma --format=lzma -9 --extreme --lzma1=preset=9,lc=1,lp=0,pb=0 t
cat ../ext/vondehi t.lzma > $OUTPUT
chmod +x $OUTPUT

FILESIZE=$(stat -c%s "$OUTPUT")
echo "Size of $OUTPUT = $FILESIZE bytes."
rm t.lzma
cd ..
